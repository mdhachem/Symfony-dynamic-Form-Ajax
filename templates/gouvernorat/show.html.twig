{% extends 'base.html.twig' %}

{% block title %}Gouvernorat{% endblock %}

{% block body %}
    <h1>Gouvernorat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gouvernorat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gouvernorat.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gouvernorat_index') }}">back to list</a>

    <a href="{{ path('gouvernorat_edit', {'id': gouvernorat.id}) }}">edit</a>

    {{ include('gouvernorat/_delete_form.html.twig') }}
{% endblock %}
